# Public frontend settings only. Do not put secrets in Vite variables.
VITE_API_BASE_URL=http://localhost:8000

# For the production Docker/nginx setup in this repository, leave this unset
# or build with VITE_API_BASE_URL=/api so nginx can proxy same-origin requests.
